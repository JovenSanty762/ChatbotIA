/** @type {import('tailwindcss').Config} */
module.exports = {
  // Escanea el panel para incluir solo las clases utilizadas.
  content: ['./static/**/*.html'],
  theme: { extend: {} },
  plugins: [],
};
